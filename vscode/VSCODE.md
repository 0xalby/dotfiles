# Keybindings
## Workspace
* Ctrl + Shift + E will focus the explorer
* Ctrl + Shift + ò will focus the terminal
* c + a + "object" will change everything inside and including the specified object
* v + i will select everything inside the closest curly braces
* v + a will select everything inside the closest curly brances including
